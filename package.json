{
  "name": "briya-room-reservation-v2",
  "version": "1.0.0",
  "private": true,
  "description": "Clean rebuild of the Briya Room Reservation application",

  "scripts": {
    "dev": "concurrently -k -n BACKEND,FRONTEND -c blue,green \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  },

  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
